## 개발 규칙

> 이 섹션을 읽지 않고 개발을 진행하면 안 된다. 모든 개발은 아래 규칙을 먼저 확인한 뒤 시작해야 한다.

### 문서화

모든 개발은 반드시 문서화를 함께 진행한다. 기능 추가, 구조 변경, 정책 변경, API 변경이 있으면 관련 문서를 즉시 갱신한다.

### 설계 원칙

- 구현은 가능한 한 잘 알려진 소프트웨어 디자인 패턴을 따른다.
- 임시 로직이나 산발적인 분기 추가보다 역할이 분명한 구조를 우선한다.
- 공통으로 재사용되는 로직은 중복 복사하지 말고 `src/lib/` 내 공통 모듈로 정리한다.
- 모듈, 서비스는 책임이 명확해야 하며 결합도는 낮추고 응집도는 높인다.

### 변경 시 검토

- 작은 기능 수정이라도 장기적으로 확장 가능한 구조인지 먼저 검토한다.
- 변경사항이 생기면 해당 파일만 국소적으로 보지 말고 전체 흐름에서 점검한다.

### 영향분석

- 모든 개발 전후에는 반드시 영향분석을 수행한다.
- 전체 성능, 개발 규칙 위반 여부, 다른 모듈 영향 여부를 함께 점검한다.

### 디자인 규칙

- 모든 개발은 공동 디자인 규칙을 먼저 확인하고 반드시 그 기준을 따른다.
- 공동 디자인 규칙은 `docs/design/README.md`를 기준 문서로 사용한다.
- 새로운 개발 내용이 공동 디자인 규칙에 없는 유형이라면 먼저 디자인 문서를 정의하거나 갱신한 뒤 개발한다.
- 문서 정의 없이 예외 구현을 먼저 추가하지 않는다.

### 용어 관리

- 프로젝트 공통 용어는 `docs/glossary/README.md`의 용어사전을 기준으로 사용한다.
- 새로운 용어, 기능명, API 개념이 생기면 먼저 또는 동시에 용어사전을 갱신한다.

### 문서 우선순위

문서 간 내용이 충돌할 경우, 아래 순서대로 상위 문서의 내용을 우선한다.

1. `CLAUDE.md` — 최우선 작업 규칙
2. `README.md` — 프로젝트 일반 사용법
3. `docs/design/README.md` — 공동 디자인 규칙
4. `docs/glossary/README.md` — 공동 용어사전
5. `docs/` — 기능/기획 문서

### Daily 기록 규칙

- 모든 작업 기록은 `docs/daily/` 아래에 날짜별로 저장한다.
- 날짜 폴더 형식: `YYYY-MM-DD` (예: `docs/daily/2026-04-04/`)
- 각 날짜 폴더에는 `claude.md` 파일을 유지한다.
- Claude는 작업 후 같은 날짜의 `docs/daily/YYYY-MM-DD/claude.md`에 내용을 누적 기록한다.
- 큰 구조 변경, 파일 이동, 삭제, 테스트 결과는 반드시 기록한다.
- 기본 운영 규칙과 예시는 `docs/daily/README.md`를 따른다.

### 개발 워크플로우

Claude 단독 환경에서는 아래 순서로 개발을 진행한다.

**1단계: 사전 조사**

- 개발 시작 전 관련 자료를 조사하고, 기존 코드베이스의 영향 범위를 파악한다.
- 필요 시 웹 검색을 통해 최신 기술 문서나 모범 사례를 확인한다.

**2단계: 영향분석 (사전)**

- 변경 대상 파일과 연관된 모듈(`src/lib/`), API Routes, Supabase 테이블, Slack 알림을 식별한다.
- 변경으로 인해 깨질 수 있는 비즈니스 흐름을 사전에 파악한다.

**3단계: 개발**

- 이 문서의 설계 원칙과 디자인 규칙을 준수하며 코드를 작성한다.
- 문서화를 코드 작성과 동시에 진행한다.

**4단계: 영향분석 (사후)**

- 변경 완료 후 전체 성능, 규칙 위반 여부, 다른 모듈 영향을 점검한다.
- webhook 수신 → AI 처리 → Supabase 저장 → LINE 응답 → Slack 알림까지의 사용자 흐름이 정상인지 확인한다.

**5단계: 기록**

- 작업 내용을 `docs/daily/YYYY-MM-DD/claude.md`에 기록한다.
- 변경된 문서가 있으면 해당 문서의 갱신 여부를 최종 확인한다.